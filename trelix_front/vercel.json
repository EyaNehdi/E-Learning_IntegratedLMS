{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
        "routes": [
        {
        "src": "/api/(.*)",
        "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
        "dest": "/api/$1"
        }
    ]
}